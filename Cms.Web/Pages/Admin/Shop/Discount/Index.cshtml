@page
@using Cms.Core.Convertors
@model Cms.Web.Pages.Admin.Shop.Discount.IndexModel

@{
    ViewData["Title"] = "لیست تخفیف ها";
    var discounts = ViewData["discounts"] as List<Cms.DataLayer.Entities.Shop.Discount>;
}


<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">لیست تخفیف ها</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>

<!-- /.row -->
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                لیست تخفیف ها
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="table-responsive">
                    <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">
                        <div class="col-md-12" style="margin:12px 0px">
                            <div class="row">
                                <a asp-page="CreateDiscount" class="btn btn-outline btn-success">افزودن تخفیف</a>
                            </div>
                        </div>
                        @if (discounts?.Count != 0)
                        {

                            <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info">
                                <thead>
                                    <tr role="row">
                                        <th style="width: 280px;">عنوان </th>
                                        <th style="width: 60px;">تخفیف</th>
                                        <th style="width: 200px;">تاریخ شروع</th>
                                        <th style="width: 200px;">تاریخ پایان</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in discounts)
                                    {

                                        <tr class="gradeA odd">
                                            <td class="sorting_1">@item.Title</td>
                                            <td class="sorting_1">@(item.Amount)% </td>
                                            <td class="sorting_1">@item.StartDate.MiladiToShamsi() </td>
                                            <td class="sorting_1">@item.EndDate.MiladiToShamsi()</td>
                                            <td>
                                                <a asp-page="EditDiscount" asp-route-id="@item.DiscountId" class="btn btn-warning btn-sm">ویرایش</a>
                                                <a asp-page="DeleteDiscount" asp-route-id="@item.DiscountId" class="btn btn-danger btn-sm">حذف</a>
                                            </td>

                                        </tr>

                                    }

                                </tbody>
                            </table>
                        }
                        else
                        {
                            <div class="help-block text-warning">تخفیفی موجود نیست</div>
                        }

                    </div>
                </div>
                <!-- /.table-responsive -->

            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
