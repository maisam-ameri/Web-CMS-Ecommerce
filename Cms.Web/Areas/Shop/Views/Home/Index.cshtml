@model IEnumerable<Cms.Core.DTOs.Shop.ProductDto>
@{
    ViewData["Title"] = "فروشگاه";
}

<main>
    <div class="container">
        <div class="user-account">
            <div class="row">

                <!-- Filters -->
                <div class="col-md-3 col-sm-4 col-xs-12">
                    <aside>
                        <section>
                            <header><h3> فیلتر ها </h3></header>
                            <div class="inner">
                                <ul>
                                    <li><a href="/UserPanel"> مشاهده حساب کابری </a></li>
                                    <li><a href="/UserPanel/EditProfile"> ویرایش حساب کابری </a></li>
                                    <li><a href="/UserPanel/EditPassword"> تغییر رمز عبور </a></li>
                                    <li><a href=""> تنظیمات حساب کاربری </a></li>
                                    <li><a href=""> خروج از حساب کاربری </a></li>
                                </ul>
                            </div>
                        </section>
                    </aside>
                </div>

                <!-- Product List -->
                <div class="col-md-9 col-sm-8 col-xs-12">
                    <section class="user-account-content">
                        <header><h1> لیست محصولات </h1></header>
                        <div class="inner">

                            <section class="terms-items">
                                <div class="row">
                                    <!-- col -->
                                    @await Component.InvokeAsync("LastProductsComponent")

                                </div>
                            </section>

                        </div>
                    </section>
                </div>

                <!-- Paggination -->
                <div class="row">
                    <div class="col-sm-6">
                    </div>
                    <div class="col-sm-6">
                        <div class="dataTables_paginate paging_simple_numbers" id="dataTables-example_paginate">
                            <ul class="pagination">
                                <li class="paginate_button previous " aria-controls="dataTables-example" tabindex="0" id="dataTables-example_previous">
                                    <a href=@($"/Admin/Users?pageid=")>قبلی</a>
                                </li>
                                @* @if (Model.Users.TotalPage > 1) *@
                                @* { *@
                                @* for (int i = 1; i <= Model.Users.TotalPage; i++) *@
                                @* { *@
                                @* var isActive = (i == Model.Users.CurrentPage); *@
                                <li class="paginate_button" aria-controls="dataTables-example" tabindex="0">
                                    <a href="/Admin/Users?pageid=1">1</a>
                                </li>

                                @* } *@
                                @* } *@
                                <li class="paginate_button next" aria-controls="dataTables-example" tabindex="0" id="dataTables-example_next"><a href=@($"/Admin/Users?pageid=")>بعدی</a></li>
                            </ul>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</main>

