@using Cms.Core.Convertors
@model IEnumerable<Cms.DataLayer.Entities.Shop.OrderDetail>
@{
    ViewData["Title"] = "سفارشات من";
}

<main>
    <div class="container">
        <div class="user-account">
            <div class="row">
                <partial name="_SideBar" />
                <div class="col-md-9 col-sm-8 col-xs-12">
                    <section class="user-account-content">
                        <header><h1>سفارشات من </h1></header>
                        <div class="inner">
                            <div class="account-information">

                                <div class="panel-body">
                                    <div class="table-responsive">
                                        <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">

                                            <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info">
                                                <thead>
                                                    <tr role="row">
                                                        <th style="width: 219px;">عنوان</th>
                                                        <th style="width: 219px;">تعداد </th>
                                                        <th style="width: 101px;">قیمت</th>
                                                        <th style="width: 219px;">تاریخ</th>
                                                        <th style="width: 101px;">جزئیات</th>
                                                    </tr>
                                                </thead>
                                                <tbody>

                                                    @foreach (var item in Model)
                                                    {

                                                        <tr class="gradeA odd">
                                                            <td class="sorting_1">@item.Title</td>
                                                            <td class="sorting_1">@item.Count</td>
                                                            <td class="sorting_1">@item.Price</td>
                                                            <td class="sorting_1">@item.RegisterDate.MiladiToShamsi()</td>
                                                            <td>
                                                                <a asp-action="DeleteOrderDetail" asp-route-id="@item.OrderDetailId" class="btn btn-danger btn-sm">حذف</a>
                                                            </td>

                                                        </tr>

                                                    }

                                                </tbody>
                                            </table>

                                        </div>
                                    </div>
                                    <!-- /.table-responsive -->

                                    <div class="input-group">
                                        جمع کل : @Model.Sum(o => o.Price)  تومان
                                    </div>
                                    @if (ViewData["isOrderFinally"] == null)
                                    {

                                        <div class="input-group">
                                            <a asp-area="UserPanel" asp-action="PayOrder" asp-route-orderId="@Model.FirstOrDefault().OrderId" class="btn btn-success form-control"> پرداخت</a>
                                        </div>

                                    }
                                </div>
                            </div>
                        </div>
                    </section>
                </div>
            </div>
        </div>
    </div>
</main>

